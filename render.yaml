services:
  - type: web
    name: mlb-bet-predictor
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: starter
    envVars:
      - key: GMAIL_APP_PASSWORD
        sync: false
      - key: RUN_TOKEN
        sync: false
